{{template "base" .}}

{{define "content"}}
  <div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 class="mt-5">welcome to the home page</h1>
            <p class="lead">this is a simple home page template using bootstrap.</p>
            <p>{{index .StringMap "author"}}</p>
            <img src="/static/images/home.jpeg" class="img-fluid" alt="Responsive image"/>
        </div>
    </div>
 </div>
{{end}}
