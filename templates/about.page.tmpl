{{template "base" .}}

{{define "content"}}
  <div class="row">
    <div class="col-md-12">
    <h1 class="mt-5">About Us</h1>
    <p class="lead">This is the about page template using Bootstrap.</p>
    </div>
  </div>
{{end}}
